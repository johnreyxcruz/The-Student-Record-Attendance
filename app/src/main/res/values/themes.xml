<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RecordAttendance" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/themeColor</item>

    </style>

    <style name="Theme.RecordAttendance" parent="Base.Theme.RecordAttendance">
        <item name="android:colorBackground">#DFDFDF</item>
    </style>
    <style name="Theme.RecordAttendance.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.RecordAttendance.AppBarOverlay"
        parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.RecordAttendance.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>